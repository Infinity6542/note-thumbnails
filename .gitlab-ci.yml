# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/user/application_security/sast/#available-cicd-variables
# Secret Detection customization: https://docs.gitlab.com/user/application_security/secret_detection/pipeline/configure/
# Dependency Scanning customization: https://docs.gitlab.com/user/application_security/dependency_scanning/dependency_scanning_sbom/#customizing-analyzer-behavior
# Container Scanning customization: https://docs.gitlab.com/user/application_security/container_scanning/#customizing-analyzer-behavior
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ci/variables/#cicd-variable-precedence
stages:
- test
- secret-detection
- build
- release

sast:
  stage: test
include:
- template: Security/SAST.gitlab-ci.yml
- template: Security/Secret-Detection.gitlab-ci.yml

variables:
  SECRET_DETECTION_ENABLED: 'true'

secret_detection:
  stage: secret-detection

build:
  stage: build
  image: node:${NODE_VERSION}
  cache:
    key: ${CI_COMMIT_REF_SLUG}-${NODE_VERSION}
    paths:
      - .npm/
  parallel:
    matrix:
      - NODE_VERSION: ["22", "24"]
  before_script:
    - npm ci --cache .npm --prefer-offline
  script:
    - npm run build --if-present
    - npm run lint
  artifacts:
    paths:
      - main.js
      - manifest.json
      - styles.css
    expire_in: 1 week

release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  needs:
    - job: "build: [24]"
      artifacts: true
  release:
    tag_name: $CI_COMMIT_TAG
    name: "Release $CI_COMMIT_TAG"
    description: "An automated build and release of $CI_COMMIT_TAG."
    script:
      - echo "Beginning release..."
    assets:
      links:
        - name: "main.js"
          url: "${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/raw/main.js?job=build:%20%5B24%5D"
        - name: "manifest.json"
          url: "${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/raw/manifest.json?job=build:%20%5B24%5D"
        - name: "styles.css"
          url: "${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/raw/styles.css?job=build:%20%5B24%5D"
  rules:
    - if: $CI_COMMIT_TAG
